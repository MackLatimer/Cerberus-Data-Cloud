# This is an example file. Copy it to .env for local development and fill in your actual values.
# The .env file is ignored by Git and should NOT be committed.

# Stripe publishable key (use your TEST key for local development)
STRIPE_PUBLISH"ABLE_KEY=pk_test_YOUR_TEST_KEY_HERE

# Backend API URL for this environment (points to a local server by default)
API_BASE_URL=http://127.0.0.1:5001/api/v1

# Default Campaign ID for this frontend
CAMPAIGN_ID=1